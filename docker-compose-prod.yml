version: "3.9"

volumes:
  data:

services:
  acclab-app:
    build: 
      context: ./
      dockerfile: Dockerfile.prod
    container_name: acclab_app
    ports:
      - 80:80
    volumes:
    - .:/app
    - /app/node_modules